import { DateRangePickDirective } from './date-range-pick.directive';

describe('DateRangePickDirective', () => {
  it('should create an instance', () => {
    const directive = new DateRangePickDirective();
    expect(directive).toBeTruthy();
  });
});
